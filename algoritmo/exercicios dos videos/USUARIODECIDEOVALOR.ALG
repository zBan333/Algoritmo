algoritmo "usuarioDecideOValor"
var
   numero, contador: inteiro
inicio
      escreva("digite um numero: ")
      leia(numero)
      
      contador <- 0
      enquanto (contador <= numero) faca
               escreva(contador)
               contador <- contador + 1
      FimEnquanto

fimalgoritmo