algoritmo "Nome do Programa"
var
    x, y: inteiro
procedimento soma(x, y: inteiro)
inicio
    escreva("A soma dos dois numeros é ", x + y)
fimProcedimento

inicio
    escreva("Qual o valor de x? ")
    leia(x)
    escreva("Qual o valor de y? ")
    leia(y)
    soma(x, y)


fimalgoritmo