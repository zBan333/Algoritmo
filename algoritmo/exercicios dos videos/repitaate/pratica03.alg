algoritmo "fatorial"
var
    contador, numero: inteiro
inicio
    escreva("digite um numero: ")
    leia(numero)
    contador <- numero

    repita
        escreval(contador)
        contador <- contador - 1
    ate (contador < 1)

fimalgoritmo